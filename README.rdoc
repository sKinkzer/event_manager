== README

=== Requirements
* Ruby 2.2
* Rails 4.2
* Node.js (tested on 0.12.4)

=== Installation

Install bundle
`bundle install`

Migrate db
`rake db:migrate`

Install front end vendor assets
`rake bower:install`

Start server
`rails s`
